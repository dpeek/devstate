{
  "name": "devstate",
  "version": "0.1.6",
  "description": "Agent-first development loop supervisor.",
  "license": "MIT",
  "bin": {
    "devstate": "./dist/cli.js"
  },
  "files": [
    "dist",
    "schema",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit && vp check --fix",
    "test": "npm run build && node --test dist",
    "test:watch": "cd src && node --test --watch \"*.test.ts\"",
    "prepack": "npm run test"
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=24.12"
  }
}
